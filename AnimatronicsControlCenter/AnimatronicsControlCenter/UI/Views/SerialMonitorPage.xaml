<Page
    x:Class="AnimatronicsControlCenter.UI.Views.SerialMonitorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cv="using:AnimatronicsControlCenter.UI.Converters"
    mc:Ignorable="d">

    <Grid Padding="12,12,12,0" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="{x:Bind ViewModel.PauseButtonText, Mode=OneWay}"
                    Command="{x:Bind ViewModel.TogglePauseCommand}"/>

            <Button Grid.Column="1"
                    Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Clear', ViewModel.Strings.Code), Mode=OneWay}"
                    Command="{x:Bind ViewModel.ClearCommand}"/>

            <TextBlock Grid.Column="2"
                       VerticalAlignment="Center"
                       Text="{x:Bind ViewModel.Strings.Get('SerialMonitor_Filter', ViewModel.Strings.Code), Mode=OneWay}"/>

            <ComboBox Grid.Column="3"
                      Width="120"
                      SelectedIndex="{x:Bind ViewModel.FilterIndex, Mode=OneWay}"
                      SelectionChanged="FilterCombo_SelectionChanged">
                <ComboBoxItem Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Filter_All', ViewModel.Strings.Code), Mode=OneWay}"
                              Tag="All"/>
                <ComboBoxItem Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Filter_Tx', ViewModel.Strings.Code), Mode=OneWay}"
                              Tag="Tx"/>
                <ComboBoxItem Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Filter_Rx', ViewModel.Strings.Code), Mode=OneWay}"
                              Tag="Rx"/>
            </ComboBox>

            <ToggleSwitch Grid.Column="4"
                          IsOn="{x:Bind ViewModel.IsAutoScrollEnabled, Mode=TwoWay}"
                          OnContent="{x:Bind ViewModel.Strings.Get('SerialMonitor_AutoScroll_On', ViewModel.Strings.Code), Mode=OneWay}"
                          OffContent="{x:Bind ViewModel.Strings.Get('SerialMonitor_AutoScroll_Off', ViewModel.Strings.Code), Mode=OneWay}"/>

            <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <TextBox Width="240"
                         PlaceholderText="{x:Bind ViewModel.Strings.Get('SerialMonitor_Search', ViewModel.Strings.Code), Mode=OneWay}"
                         Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Prev', ViewModel.Strings.Code), Mode=OneWay}"
                        Command="{x:Bind ViewModel.FindPreviousCommand}"/>
                <Button Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Next', ViewModel.Strings.Code), Mode=OneWay}"
                        Command="{x:Bind ViewModel.FindNextCommand}"/>
            </StackPanel>

            <Button Grid.Column="6"
                    Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_CopyAll', ViewModel.Strings.Code), Mode=OneWay}"
                    Command="{x:Bind ViewModel.CopyAllCommand}"/>

            <Button Grid.Column="7"
                    Content="{x:Bind ViewModel.Strings.Get('SerialMonitor_Save', ViewModel.Strings.Code), Mode=OneWay}"
                    Click="SaveButton_Click"/>
        </Grid>

        <Pivot x:Name="MainPivot"
               Grid.Row="1"
               SelectionChanged="MainPivot_SelectionChanged">
            <PivotItem Header="{x:Bind ViewModel.Strings.Get('SerialMonitor_Tab_Serial', ViewModel.Strings.Code), Mode=OneWay}">
                <Grid>
                    <ScrollViewer x:Name="SerialScroll"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                        <ScrollViewer.Resources>
                            <cv:SerialDirectionToBrushConverter x:Key="SerialDirectionToBrushConverter"/>
                        </ScrollViewer.Resources>

                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Vertical"/>
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="models:SerialTrafficEntry" xmlns:models="using:AnimatronicsControlCenter.Core.Models">
                                    <Grid ColumnSpacing="8" Padding="0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="86"/>
                                            <ColumnDefinition Width="14"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{x:Bind TimestampText}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                   TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{x:Bind DirectionArrow}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="{Binding Direction, Converter={StaticResource SerialDirectionToBrushConverter}}"
                                                   TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{x:Bind Line}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   Foreground="{Binding Direction, Converter={StaticResource SerialDirectionToBrushConverter}}"
                                                   TextWrapping="NoWrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </ScrollViewer>
                </Grid>
            </PivotItem>

            <PivotItem Header="{x:Bind ViewModel.Strings.Get('SerialMonitor_Tab_Packets', ViewModel.Strings.Code), Mode=OneWay}">
                <Grid ColumnSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
                        <TextBlock Text="{x:Bind ViewModel.Strings.Get('SerialMonitor_ParseErrors', ViewModel.Strings.Code), Mode=OneWay}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind ViewModel.ParseErrorCount, Mode=OneWay}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="방향" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.PacketDirectionFilters, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPacketDirectionFilter, Mode=TwoWay}"
                                  MinWidth="100"/>
                        <TextBlock Text="송신 ID" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.PacketSrcIdFilters, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPacketSrcIdFilter, Mode=TwoWay}"
                                  MinWidth="80"/>
                        <TextBlock Text="수신 ID" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.PacketTarIdFilters, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPacketTarIdFilter, Mode=TwoWay}"
                                  MinWidth="80"/>
                        <TextBlock Text="Command" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.PacketCommandFilters, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPacketCommandFilter, Mode=TwoWay}"
                                  MinWidth="140"/>
                        <TextBlock Text="Status" VerticalAlignment="Center"/>
                        <ComboBox ItemsSource="{x:Bind ViewModel.PacketStatusFilters, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedPacketStatusFilter, Mode=TwoWay}"
                                  MinWidth="100"/>
                        <ToggleSwitch Header="오류만"
                                      IsOn="{x:Bind ViewModel.IsParseErrorOnly, Mode=TwoWay}"/>
                    </StackPanel>

                    <ListView x:Name="PacketList"
                              Grid.Row="1"
                              Grid.Column="0"
                              ItemsSource="{x:Bind ViewModel.Packets, Mode=OneWay}"
                              SelectedItem="{x:Bind ViewModel.SelectedPacket, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="vm:PacketItem" xmlns:vm="using:AnimatronicsControlCenter.UI.ViewModels">
                                <TextBlock Text="{x:Bind Summary}"
                                           FontFamily="Consolas"
                                           TextWrapping="NoWrap"
                                           TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Grid Grid.Row="1" Grid.Column="1" RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0"
                                   Text="{Binding SelectedPacket.ParseError, TargetNullValue=''}"/>
                        <TextBox Grid.Row="1"
                                 Text="{Binding SelectedPacket.PrettyJson, TargetNullValue=''}"
                                 FontFamily="Consolas"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
                    </Grid>
                </Grid>
            </PivotItem>

            <PivotItem Header="{x:Bind ViewModel.Strings.Get('SerialMonitor_Tab_ComRaw', ViewModel.Strings.Code), Mode=OneWay}">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
                        <ToggleSwitch IsOn="{x:Bind ViewModel.IsComRawCaptureEnabled, Mode=TwoWay}"
                                      OnContent="{x:Bind ViewModel.Strings.Get('SerialMonitor_ComRaw_On', ViewModel.Strings.Code), Mode=OneWay}"
                                      OffContent="{x:Bind ViewModel.Strings.Get('SerialMonitor_ComRaw_Off', ViewModel.Strings.Code), Mode=OneWay}"/>
                        <TextBlock VerticalAlignment="Center"
                                   Text="{x:Bind ViewModel.Strings.Get('SerialMonitor_ComRaw_Desc', ViewModel.Strings.Code), Mode=OneWay}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <ScrollViewer x:Name="ComRawScroll"
                                  Grid.Row="1"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                        <ScrollViewer.Resources>
                            <cv:SerialDirectionToBrushConverter x:Key="SerialDirectionToBrushConverter"/>
                        </ScrollViewer.Resources>

                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.ComRawEntries, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Vertical"/>
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="models:SerialTrafficEntry" xmlns:models="using:AnimatronicsControlCenter.Core.Models">
                                    <Grid ColumnSpacing="8" Padding="0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="86"/>
                                            <ColumnDefinition Width="14"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{x:Bind TimestampText}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                   TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{x:Bind DirectionArrow}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   FontWeight="SemiBold"
                                                   Foreground="{Binding Direction, Converter={StaticResource SerialDirectionToBrushConverter}}"
                                                   TextWrapping="NoWrap"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{x:Bind Line}"
                                                   FontFamily="Consolas"
                                                   FontSize="12"
                                                   Foreground="{Binding Direction, Converter={StaticResource SerialDirectionToBrushConverter}}"
                                                   TextWrapping="NoWrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </ScrollViewer>
                </Grid>
            </PivotItem>
        </Pivot>

        <!-- Status bar -->
        <Border x:Name="statusbar"
                Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,5"
                Margin="-12,0,-12,0">
            <StackPanel Orientation="Horizontal" Spacing="16">
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse Width="7" Height="7" Fill="#4CC180"/>
                    <TextBlock Text="연결됨" FontSize="12"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                </StackPanel>
                <StackPanel x:Name="StatusBarCounts"
                            Orientation="Horizontal"
                            Spacing="16"
                            VerticalAlignment="Center">
                    <StackPanel.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="카운터 초기화"
                                            Command="{x:Bind ViewModel.ClearCountsCommand}"/>
                        </MenuFlyout>
                    </StackPanel.ContextFlyout>
                    <TextBlock VerticalAlignment="Center" FontSize="12" FontFamily="Consolas"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                        <Run Text="TX "/>
                        <Run Text="{x:Bind ViewModel.TxCount, Mode=OneWay}"
                             FontWeight="SemiBold"
                             Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    </TextBlock>
                    <TextBlock VerticalAlignment="Center" FontSize="12" FontFamily="Consolas"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                        <Run Text="RX "/>
                        <Run Text="{x:Bind ViewModel.RxCount, Mode=OneWay}"
                             FontWeight="SemiBold"
                             Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    </TextBlock>
                    <TextBlock VerticalAlignment="Center" FontSize="12" FontFamily="Consolas"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}">
                        <Run Text="Total "/>
                        <Run Text="{x:Bind ViewModel.TotalCount, Mode=OneWay}"
                             FontWeight="SemiBold"
                             Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                    </TextBlock>
                </StackPanel>
                <!-- LIVE indicator (not paused) -->
                <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center"
                            Visibility="{x:Bind BoolToCollapsed(ViewModel.IsPaused), Mode=OneWay}">
                    <Ellipse Width="6" Height="6" Fill="#4CC180"/>
                    <TextBlock Text="LIVE" FontSize="12" FontWeight="SemiBold" Foreground="#4CC180"/>
                </StackPanel>
                <!-- Paused indicator -->
                <TextBlock Text="● 일시정지"
                           FontSize="12" FontWeight="SemiBold"
                           Foreground="{ThemeResource SystemAccentColor}"
                           VerticalAlignment="Center"
                           Visibility="{x:Bind BoolToVisible(ViewModel.IsPaused), Mode=OneWay}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>


