<Page
    x:Class="AnimatronicsControlCenter.UI.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AnimatronicsControlCenter.Core.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right" Background="Transparent" IsOpen="False">
            <AppBarButton Icon="Refresh" Label="Scan Devices" Command="{x:Bind ViewModel.ScanCommand}"/>
            <CommandBar.Content>
                <TextBlock Text="Devices" Style="{StaticResource TitleTextBlockStyle}" Margin="12,14,0,0"/>
            </CommandBar.Content>
        </CommandBar>

        <GridView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Devices}" Padding="12"
                  IsItemClickEnabled="True" ItemClick="GridView_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Device">
                    <Grid Width="200" Height="150" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{x:Bind Id}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="{x:Bind StatusMessage}" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
        <ProgressBar Grid.Row="1" IsIndeterminate="True" Visibility="{x:Bind BoolToVisibility(ViewModel.IsScanning), Mode=OneWay}" VerticalAlignment="Top"/>
    </Grid>
</Page>

